window.addEventListener('load', () => {
      const text = document.querySelector('.loading');
      const bg = document.querySelector('.loader');

      text.classList.add('loadedText');
      bg.classList.add('loadedBG');
})